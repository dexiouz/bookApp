import { Text, View, StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
   title: {
     fontSize: 24,
     fontWeight: '600',
     marginVertical: 10
   },
   bookTitle: {
    fontSize: 24,
    fontWeight: '600'
  }
});